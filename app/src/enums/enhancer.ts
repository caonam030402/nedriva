export enum EUpscaleModel {
  Prime = 'prime',
  Gentle = 'gentle',
  OldPhoto = 'old_photo',
  TryAll = 'try_all',
  More = 'more',
}

export enum EMoreModel {
  Balanced = 'balanced',
  Ultra = 'ultra',
  Strong = 'strong',
  DigiArt = 'digi_art',
  Magic = 'magic',
}

export enum ESizeMode {
  Auto = 'auto',
  Scale = 'scale',
  Custom = 'custom',
}

export enum EScaleFactor {
  X1 = 1,
  X2 = 2,
  X4 = 4,
  X8 = 8,
  X16 = 16,
}

export enum EBgType {
  General = 'general',
  Car = 'car',
}

export enum EQueueStatus {
  Ready = 'ready',
  Processing = 'processing',
  Done = 'done',
  Error = 'error',
}
