/**
 * Runtime constants for the enhancer-video pipeline.
 */

/** Max retry delay (ms) for video job status polling — cap of exponential back-off. */
export const VIDEO_JOB_RETRY_DELAY_MAX_MS = 10_000;
